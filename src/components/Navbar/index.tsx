import './styles.css';

const Navbar = () => {

    return (
        <nav className="bg-primary">
            <div className="navbar-container">
                <a href="#link">Github API</a>
            </div>
        </nav>
    );
}

export default Navbar;